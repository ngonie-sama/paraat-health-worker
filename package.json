{
  "name": "paraat-health-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker that health-checks Paraat LLM providers (via the CF AI Gateway) and RAG/custom agent endpoints, alerting on outages.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
